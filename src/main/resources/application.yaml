spring:
  application:
    name: demo
  datasource:
    url: jdbc:postgresql://localhost:5432/java-demo?currentSchema=public
    username: postgres
    password: 123
    driver-class-name: org.postgresql.Driver
server:
  port: 8081
springdoc:
  swagger-ui:
    path: /swagger
  api-docs:
    path: /api-docs